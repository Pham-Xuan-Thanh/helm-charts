apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "biploma.fullname" . }}-test-connection"
  labels:
    {{- include "biploma.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "biploma.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
