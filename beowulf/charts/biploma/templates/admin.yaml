{{- if .Values.admin.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "biploma.fullname" . }}-admin
  labels:
    app: {{ include "biploma.fullname" . }}-admin
    chart: {{ include "biploma.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    {{ with .Values.admin.annotations  -}}
  annotations:
    {{ toYaml . | nindent 4 }} 
  {{- end  }}
spec:
  replicas: {{ .Values.admin.replicaCount }}
  minReadySeconds: 5
  strategy:
    # indicate which strategy we want for rolling update
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 2
      maxUnavailable: 1
  selector:
    matchLabels:
      app: {{ include "biploma.fullname" . }}
      name: {{ include "biploma.fullname" . }}-admin
      release: {{ .Release.Name }}
  template:
    metadata: 
      labels:
        app: {{ include "biploma.fullname" . }}
        name: {{ include "biploma.fullname" . }}-admin
        release: {{ .Release.Name }}
      {{ with .Values.admin.podAnnotations  -}}
      annotations:
      {{ toYaml . | nindent 8 }} 
    {{- end  }}
    
    spec:
{{- if .Values.admin.nodeSelector }}
      nodeSelector:
{{ toYaml .Values.admin.nodeSelector | indent 8 }}
{{- end }}
{{- if .Values.admin.tolerations }}
      tolerations:
{{ toYaml .Values.admin.tolerations | indent 8 }}
{{- end }}
{{- if .Values.admin.affinity }}
      affinity:
{{ toYaml .Values.admin.affinity | indent 8 }}
{{- end }}
      {{- with .Values.web.imagePullSecrets }}
      imagePullSecrets:
        {{- range $secret :=  .  }}
        - name: {{ $secret }}
        {{- end }}
      {{- end }} 
      containers:
      - image: "{{ .Values.admin.image.repository }}:{{ .Values.admin.image.tag }}"
        imagePullPolicy: {{ .Values.admin.image.pullPolicy }}
        name: {{ include "biploma.fullname" . }}-admin
        securityContext:
          runAsNonRoot: false
          allowPrivilegeEscalation: true
          privileged: true
          readOnlyRootFilesystem: true
          capabilities:
            add:
              - NET_BIND_SERVICE
            drop:
              - all
        env:
          {{- if .Values.admin.extraEnvs }}
          {{- toYaml .Values.admin.extraEnvs | nindent 10 }}
          {{- end }}
        {{ if .Values.admin.extraEnvVarsSecret }}
        envFrom:
          {{ toYaml .Values.admin.extraEnvVarsSecret | nindent 10}}
        {{ end }}
        resources:
{{ toYaml .Values.admin.resources | indent 12 }}
        ports:
        - containerPort: {{ .Values.admin.containerPort }}
        {{- if .Values.admin.livenessProbe.enabled }}
        livenessProbe:
          httpGet:
            scheme: "HTTP"
            path: /
            port: {{ .Values.admin.containerPort }}
          initialDelaySeconds: {{ .Values.admin.livenessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.admin.livenessProbe.periodSeconds }}
          timeoutSeconds: {{ .Values.admin.livenessProbe.timeoutSeconds }}
          successThreshold: {{ .Values.admin.livenessProbe.successThreshold }}
          failureThreshold: {{ .Values.admin.livenessProbe.failureThreshold }}
        {{- end }}
        {{- if .Values.admin.readinessProbe.enabled }}
        readinessProbe:
          httpGet:
            scheme: "HTTP"
            path: /
            port: {{ .Values.admin.containerPort }}
          initialDelaySeconds: {{ .Values.admin.readinessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.admin.readinessProbe.periodSeconds }}
          timeoutSeconds: {{ .Values.admin.readinessProbe.timeoutSeconds }}
          successThreshold: {{ .Values.admin.readinessProbe.successThreshold }}
          failureThreshold: {{ .Values.admin.readinessProbe.failureThreshold }}
        {{- end }}
        volumeMounts:
        - mountPath: /var/run
          name: tmpfs-1
        - mountPath: /var/cache/nginx
          name: tmpfs-2
      volumes:
        - name: tmpfs-1
          emptyDir: {}
        - name: tmpfs-2
          emptyDir: {}
        
      {{- if .Values.admin.securityContext }}
      securityContext:
      {{- toYaml .Values.admin.securityContext | nindent 10 }}
      {{- end }}
      
{{- end }}
